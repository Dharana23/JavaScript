let val;

// pi
val = Math.PI;

//Rounding up numbers
val = Math.round(2.8);
val = Math.ceil(2.4);
val = Math.floor(2.7);

// suare root
val = Math.sqrt(64);

// absolute value
val = Math.abs(-3);

// exponential 
val = Math.pow(8, 2);

// Max & Min
val = Math.min(2, 33, 4, 1, 55, 6, 3);
val = Math.max(2, 33, 4, 1, 55, 6, 3);

// random value
val = Math.random();

console.log(val);
